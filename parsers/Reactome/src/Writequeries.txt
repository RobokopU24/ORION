[
    null,
    "MATCH (a:Complex)-[r:summation]->(b:Summation) SET a.summation = b.displayName",
    "MATCH (a:Reaction)-[r:summation]->(b:Summation) SET a.summation = b.displayName",
    "MATCH (a:Reaction)-[r:catalystActivity]->(b:CatalystActivity) SET a.catalystactivity = b.displayName",
    "MATCH (a:CatalystActivity)-[r:activity]->(b:GO_MolecularFunction) SET b.catalystactivity = a.displayName",
    "MATCH (a:Reaction)-[r:literatureReference]->(b:Publication) SET a.publication = b.displayName+'. '+b.journal+'. '+b.year+', '+b.volume+', '+b.pages",
    "MATCH (a:Pathway)-[r:summation]->(b:Summation) SET a.summation = b.displayName",
    "MATCH (a:CatalystActivity)-[r:physicalEntity]->(b:Complex) SET b.catalystactivity = a.displayName",
    "MATCH (a:Pathway)-[r:literatureReference]->(b:Publication) SET a.publication = b.displayName+'. '+b.journal+'. '+b.year+', '+b.volume+', '+b.pages",
    "MATCH (a:BlackBoxEvent)-[r:summation]->(b:Summation) SET a.summation = b.displayName",
    "MATCH (a:BlackBoxEvent)-[r:literatureReference]->(b:Publication) SET a.publication = b.displayName+'. '+b.journal+'. '+b.year+', '+b.volume+', '+b.pages",
    "MATCH (a:CatalystActivity)-[r:physicalEntity]->(b:DefinedSet) SET b.catalystactivity = a.displayName",
    "MATCH (a:Complex)-[r:literatureReference]->(b:Publication) SET a.publication = b.displayName+'. '+b.journal+'. '+b.year+', '+b.volume+', '+b.pages",
    "MATCH (a:EntityWithAccessionedSequence)-[r:literatureReference]->(b:Publication) SET a.publication = b.displayName+'. '+b.journal+'. '+b.year+', '+b.volume+', '+b.pages",
    "MATCH (a:EntityWithAccessionedSequence)-[r:hasModifiedResidue]->(b:GeneticallyModifiedResidue) SET a.geneticallymodifiedresidue = b.displayName",
    "MATCH (a:FailedReaction)-[r:literatureReference]->(b:Publication) SET a.publication = b.displayName+'. '+b.journal+'. '+b.year+', '+b.volume+', '+b.pages",
    "MATCH (a:Drug)-[r:literatureReference]->(b:Publication) SET a.publication = b.displayName+'. '+b.journal+'. '+b.year+', '+b.volume+', '+b.pages",
    "MATCH (a:EntityWithAccessionedSequence)-[r:summation]->(b:Summation) SET a.summation = b.displayName",
    "MATCH (a:Polymerisation)-[r:summation]->(b:Summation) SET a.summation = b.displayName",
    null,
    "MATCH (a:Polymerisation)-[r:literatureReference]->(b:Publication) SET a.publication = b.displayName+'. '+b.journal+'. '+b.year+', '+b.volume+', '+b.pages",
    "MATCH (a:Polymerisation)-[r:catalystActivity]->(b:CatalystActivity) SET a.catalystactivity = b.displayName",
    "MATCH (a:Depolymerisation)-[r:summation]->(b:Summation) SET a.summation = b.displayName",
    "MATCH (a:SimpleEntity)-[r:summation]->(b:Summation) SET a.summation = b.displayName",
    "MATCH (a:SimpleEntity)-[r:literatureReference]->(b:Publication) SET a.publication = b.displayName+'. '+b.journal+'. '+b.year+', '+b.volume+', '+b.pages"
]