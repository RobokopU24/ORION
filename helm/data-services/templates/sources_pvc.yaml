apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ds-sources-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.dataServices.sourcesVolume.size }}
  storageClassName: basic